<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Диск по умолчанию
    |--------------------------------------------------------------------------
    | Этот диск используется, когда не указано имя диска явно.
    */
    'default' => 'local',

    /*
    |--------------------------------------------------------------------------
    | Конфигурация дисков
    |--------------------------------------------------------------------------
    | Каждый диск имеет свой драйвер, корневую директорию и настройки.
    | ВАЖНО: Ключ 'root' должен быть переопределен в конфигурации приложения,
    | так как ядро не знает абсолютный путь к корню вашего проекта.
    */
    'disks' => [
        // Приватный диск для внутренних файлов (логи, временные файлы)
        'local' => [
            'driver' => 'local',
            // Ядро предоставляет заглушку, приложение обязано её переопределить
            'root' => 'storage/app', 
            'visibility' => 'private',
        ],

        // Публичный диск для файлов, доступных по URL
        'public' => [
            'driver' => 'local',
            'root' => 'public/storage',
            'visibility' => 'public',
            'url' => '/storage',
        ],
    ],
];